apiVersion: kubeflow.org/v1alpha1
kind: PodDefault
metadata:
  name: vscode
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "user-space.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      orbit/vscode: ''
  desc: Start VSCode
