apiVersion: kubeflow.org/v1alpha1
kind: PodDefault
metadata:
  name: orbit-example-poddfault
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "user-space.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      orbit/orbit-example-poddfault: ''
  desc: Example PodDefault Jupyter Notebook
