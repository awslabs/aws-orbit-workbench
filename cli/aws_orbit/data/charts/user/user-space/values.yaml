# Default values for user-space.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Install parameters when user logs in the first time.
# Should be coming from the namespace labels
user: placeholder
user_email: placeholder@amz.com
namespace: placeholder
user_efsapid: placeholder

# Team paraemeters
team: ${team}
env: ${env_name}
region: ${region}
nameOverride: "user-space"
fullnameOverride: ""
efsid: ${efsid}
team_efsapid: ${efsapid}
cluster_pod_security_group_id: ${cluster_pod_security_group_id}
team_security_group_id: ${team_security_group_id}
grant_sudo: ${grant_sudo}
sts_ep: ${sts_ep}
fsGroup: 100
team_role_arn: ${team_role_arn}
K8Admin: ${K8Admin}
jupyter_user_image: ${jupyter_user_image}