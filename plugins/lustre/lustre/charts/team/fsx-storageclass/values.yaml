# Default values.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

namespace: ${team}
team: ${team}
nameOverride: ${plugin_id}
fullnameOverride: ""
serviceAccountName: default-editor
subnet: ${subnet}
sg: ${sg}
s3importpath: ${s3importpath}
s3exportpath: ${s3exportpath}
deploymentType: ${deploymentType}
storageClassName: fsx-lustre-${team}-${plugin_id}
storage: ${storage}
folder: ${folder}
fs_name: lustre-${team}-fs-${plugin_id}
image_repository: ${k8s_utilities_image}